APP_ENV=local
LOG_LEVEL=INFO

# Leave FOUNDRY_PROJECT_ENDPOINT empty to run in MOCK mode (no Azure needed).
FOUNDRY_PROJECT_ENDPOINT=
FOUNDRY_MODEL_NAME=gpt-4o

# Durable memory in production (unused in mock mode).
COSMOS_CONNECTION_STRING=
